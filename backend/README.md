# My Parlour Backend
Spring Boot 3.5 / Java 17 REST API.

Default local admin: `admin` / `admin123` (change ADMIN_USERNAME and ADMIN_PASSWORD in production).

H2 is the local default. PostgreSQL can be used by setting DB_URL, DB_DRIVER, DB_USERNAME and DB_PASSWORD.
